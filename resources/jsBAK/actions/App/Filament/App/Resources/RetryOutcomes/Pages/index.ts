import ListRetryOutcomes from './ListRetryOutcomes'
import ViewRetryOutcome from './ViewRetryOutcome'
import EditRetryOutcome from './EditRetryOutcome'

const Pages = {
    ListRetryOutcomes: Object.assign(ListRetryOutcomes, ListRetryOutcomes),
    ViewRetryOutcome: Object.assign(ViewRetryOutcome, ViewRetryOutcome),
    EditRetryOutcome: Object.assign(EditRetryOutcome, EditRetryOutcome),
}

export default Pages