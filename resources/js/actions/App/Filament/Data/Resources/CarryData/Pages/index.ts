import ListCarryData from './ListCarryData'
import CreateCarryData from './CreateCarryData'
import EditCarryData from './EditCarryData'

const Pages = {
    ListCarryData: Object.assign(ListCarryData, ListCarryData),
    CreateCarryData: Object.assign(CreateCarryData, CreateCarryData),
    EditCarryData: Object.assign(EditCarryData, EditCarryData),
}

export default Pages