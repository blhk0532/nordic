import ListTerminalLogs from './ListTerminalLogs'
import ViewTerminalLog from './ViewTerminalLog'

const Pages = {
    ListTerminalLogs: Object.assign(ListTerminalLogs, ListTerminalLogs),
    ViewTerminalLog: Object.assign(ViewTerminalLog, ViewTerminalLog),
}

export default Pages