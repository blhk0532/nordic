import AcceptInvite from './AcceptInvite'
import RegisterTenant from './RegisterTenant'
import EditTenant from './EditTenant'
import TenantMembers from './TenantMembers'

const Pages = {
    AcceptInvite: Object.assign(AcceptInvite, AcceptInvite),
    RegisterTenant: Object.assign(RegisterTenant, RegisterTenant),
    EditTenant: Object.assign(EditTenant, EditTenant),
    TenantMembers: Object.assign(TenantMembers, TenantMembers),
}

export default Pages