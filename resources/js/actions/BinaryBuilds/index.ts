import FilamentFailedJobs from './FilamentFailedJobs'
import CommandRunner from './CommandRunner'

const BinaryBuilds = {
    FilamentFailedJobs: Object.assign(FilamentFailedJobs, FilamentFailedJobs),
    CommandRunner: Object.assign(CommandRunner, CommandRunner),
}

export default BinaryBuilds