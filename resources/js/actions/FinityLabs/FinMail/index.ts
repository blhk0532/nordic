import Clusters from './Clusters'
import Resources from './Resources'

const FinMail = {
    Clusters: Object.assign(Clusters, Clusters),
    Resources: Object.assign(Resources, Resources),
}

export default FinMail