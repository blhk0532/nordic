import ListEmails from './ListEmails'
import ViewEmail from './ViewEmail'

const Pages = {
    ListEmails: Object.assign(ListEmails, ListEmails),
    ViewEmail: Object.assign(ViewEmail, ViewEmail),
}

export default Pages