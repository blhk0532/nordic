import CmsPageRenderer from './CmsPageRenderer'
import SearchResults from './SearchResults'

const Livewire = {
    CmsPageRenderer: Object.assign(CmsPageRenderer, CmsPageRenderer),
    SearchResults: Object.assign(SearchResults, SearchResults),
}

export default Livewire