import ListNotifications from './ListNotifications'
import ViewNotification from './ViewNotification'

const Pages = {
    ListNotifications: Object.assign(ListNotifications, ListNotifications),
    ViewNotification: Object.assign(ViewNotification, ViewNotification),
}

export default Pages