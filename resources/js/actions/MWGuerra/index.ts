import WebTerminal from './WebTerminal'
import FileManager from './FileManager'

const MWGuerra = {
    WebTerminal: Object.assign(WebTerminal, WebTerminal),
    FileManager: Object.assign(FileManager, FileManager),
}

export default MWGuerra