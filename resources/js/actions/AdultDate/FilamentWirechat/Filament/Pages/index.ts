import ChatDashboard from './ChatDashboard'
import ChatsPage from './ChatsPage'
import ChatPage from './ChatPage'
import FullWidthChatPage from './FullWidthChatPage'

const Pages = {
    ChatDashboard: Object.assign(ChatDashboard, ChatDashboard),
    ChatsPage: Object.assign(ChatsPage, ChatsPage),
    ChatPage: Object.assign(ChatPage, ChatPage),
    FullWidthChatPage: Object.assign(FullWidthChatPage, FullWidthChatPage),
}

export default Pages