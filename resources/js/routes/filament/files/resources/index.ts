import searches from './searches'
import fileSystemItems from './file-system-items'

const resources = {
    searches: Object.assign(searches, searches),
    fileSystemItems: Object.assign(fileSystemItems, fileSystemItems),
}

export default resources